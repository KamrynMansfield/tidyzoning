#' Title
#'
#' @param tidyzoning A tidyzoning district
#'
#' @returns a data frame with the zoning requirements in that districts
#' @export
#'
#' @examples
get_raw_zoning_req <- function(tidyzoning){
  # ozfs_variable is a data frame I will probably use
  # it has each variable along with the full name of that variable
  # I would need to update it every time there is a new variable

}
